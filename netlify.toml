# GammonGuru - Netlify Configuration
[build]
  publish = "public"
  functions = "functions"

[dev]
  publish = "public"
  functions = "functions"
  port = 8888

# Redirects - API routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
